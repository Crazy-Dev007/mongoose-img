<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Image Uploading</title>
</head>

<body>
    <h4>To Upload Image on mongoDB</h4>
    <hr>
    <div class="container-fluid  bg-dark text-light " style="display: grid; place-content: center;">
        <form action="/" method="POST" enctype="multipart/form-data">
            <div class="col-6 p-1">
                <div class="row mb-3">
                    <label for="name" class="form-label">Image Title</label>
                    <input type="text" class="form-control-sm" id="name" placeholder="Name" value="" name="name"
                        required>
                </div>
                <div class="row mb-3">
                    <label for="desc">Image Description</label>
                    <textarea id="desc" name="desc" value="" rows="2" placeholder="Description" required>
                    </textarea>
                </div>
                <div class="row mb-3">
                    <label for="image">Upload Image</label>
                    <input type="file" id="image" name="image" value="" required>
                </div>
                <div class="row mb-3">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </div>

        </form>
    </div>

    <hr>

    <h1>Uploaded Images</h1>
    <div class="container-fluid d-flex gap-1 mb-3">
        <% items.forEach(function(image) { %>
            <!-- bootstrape code -->
            <div class="card bg-black text-light" style="width: 18rem;">
                <img src="data:image/<%=image.img.contentType%>;base64,
                <%=image.img.data.toString('base64')%>" class="card-img-top" alt="Error">
                <div class="card-body">
                    <h5 class="card-title">
                        <%= image.name %>
                    </h5>
                    <p class="card-text">
                        <%= image.desc %>
                    </p>

                </div>
            </div>
            <!-- ends -->
            <% }) %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>